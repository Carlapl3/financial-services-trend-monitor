# ── API Keys (required) ───────────────────────────────────────────────
OPENAI_API_KEY=your_openai_api_key_here
FIRECRAWL_API_KEY=your_firecrawl_api_key_here

# ── Email / SMTP (required for digest and alert delivery) ─────────────
# All four SMTP vars must be set for email to work.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here
EMAIL_FROM=your_email@gmail.com

# Comma- or semicolon-separated list of recipients.
# Used by weekly digest and high-impact alert commands.
EMAIL_TO=recipient@example.com

# ── Feedback (optional) ──────────────────────────────────────────────
# Base URL of the feedback server. Feedback links in digests are only
# generated when this is set.
# FEEDBACK_BASE_URL=http://localhost:8001

# Override the email identity embedded in feedback links.
# Defaults to the first address in EMAIL_TO.
# FEEDBACK_RECIPIENT_EMAIL=
